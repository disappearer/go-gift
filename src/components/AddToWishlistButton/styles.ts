import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wishlistButton: {
    position: 'absolute',
    top: 402,
    right: 16,
    opacity: 0.8,
    backgroundColor: '#fff',
    paddingVertical: 5,
    paddingLeft: 12,
    paddingRight: 5,
    borderRadius: 15,
    flexDirection: 'row',
    alignItems: 'center',
  },
});
