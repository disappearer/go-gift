import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  productInfo: {
    flex: 1,
    width: '100%',
    paddingTop: 30,
    paddingBottom: 70,
    paddingHorizontal: 16,
  },
});
